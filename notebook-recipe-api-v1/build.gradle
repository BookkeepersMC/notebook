version = getSubprojectVersion(project)

loom {
	accessWidenerPath = file("src/main/resources/notebook-recipe-api-v1.accesswidener")
}

moduleDependencies(project, [
    'notebook-api-base',
	'notebook-networking-api-v1'
])
