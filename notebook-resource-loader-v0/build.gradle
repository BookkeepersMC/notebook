version = getSubprojectVersion(project)

loom {
	accessWidenerPath = file("src/main/resources/notebook-resource-loader-v0.accesswidener")
}

moduleDependencies(project, ['notebook-api-base'])

testDependencies(project, [
    ':notebook-lifecycle-events-v1',
	':notebook-api-base',
	':notebook-resource-loader-v0'
])
